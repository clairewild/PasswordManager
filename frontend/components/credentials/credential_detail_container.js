import { connect } from 'react-redux';
import { withRouter } from 'react-router';

import CredentialDetail from './credential_detail.jsx';

export default connect(null, null)(withRouter(CredentialDetail));
